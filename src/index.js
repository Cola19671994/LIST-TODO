import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./index.css"; // Подключение общих стилей (если нужно)

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
